spring:
 application:
  name: gateway-service
 cloud:
  config:
   discovery:
    enable: true
    serviceId: config-server
  gateway:
   routes:
    - id: user-service
      uri: http://localhost:8084
      predicates:
        - Path=/user-service
server:
 port: 8085
